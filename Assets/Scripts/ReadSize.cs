using System.Collections;
using System.Collections.Generic;
using UnityEngine;

///</summary>
/// Read the size of the object that the script is attached to.
///<summary>
public class ReadSize : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
